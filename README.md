# chromatic_api
A module to make using Google's APIs easier. Anything you can in a Google product with mouse and keyboard you can do through Python.

## Setup
To use the module, need to first setup a project using Google Cloud Console.


## To do
Convert all references to index to column letter instead